# TODO: Вернуть позже

## SearchResults — FavoriteButton
Добавить `FavoriteButton` в результаты поиска (`components/SongSearchInput.vue`). Сейчас в `main`-версии его нет (раньше был в `pages/index.vue` в inline-поиске).

Нужно:
1. В `SongSearchInput.vue` в шаблон `.result-item` добавить `<FavoriteButton :song-number="result.n" />`
2. Импортировать `FavoriteButton` в `SongSearchInput.vue`
