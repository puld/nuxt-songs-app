/**
 * История версий приложения — данные и чистые функции без Vue.
 *
 * Показывается на `/about` в режиме разработчика. Смысл списка — ответить на
 * вопрос «что изменилось с прошлого раза» тому, у кого приложение обновилось
 * само: обновление PWA проходит незаметно, и заметить новую функцию иначе
 * можно только случайно.
 *
 * Формулировки — пользовательские, а не коммит-сообщения: «раздел сборника на
 * странице песни», а не «Фаза 7.2». Записи, которые пользователю ничего не
 * говорят (рефакторинг, тесты), в список не попадают вовсе.
 *
 * Порядок — свежая версия первой. Дата в ISO (`YYYY-MM-DD`): показывается она
 * через `formatChangelogDate`, а хранить удобнее сортируемый формат.
 */

/**
 * @typedef {{version: string, date: string, changes: string[]}} ChangelogEntry
 */

/** @type {ChangelogEntry[]} */
export const CHANGELOG = [
    {
        version: '1.9.0',
        date: '2026-08-24',
        changes: [
            'Порядок подборок в меню можно менять вручную: кнопка «Порядок» рядом со списком, стрелки и перетаскивание за ручку'
        ]
    },
    {
        version: '1.8.0',
        date: '2026-08-24',
        changes: [
            'Список изменений по версиям на этой странице'
        ]
    },
    {
        version: '1.7.0',
        date: '2026-08-24',
        changes: [
            'Раздел сборника под названием песни — ссылка открывает список всех песен на нужном разделе'
        ]
    },
    {
        version: '1.6.0',
        date: '2026-08-23',
        changes: [
            'Недавно открытые песни на главной'
        ]
    },
    {
        version: '1.5.4',
        date: '2026-08-23',
        changes: [
            'Повторы в шести песнях снова показываются разметкой, а не слешами в тексте'
        ]
    },
    {
        version: '1.5.3',
        date: '2026-08-23',
        changes: [
            'Поиск больше не находит песни по пометкам для исполнителя в квадратных скобках'
        ]
    },
    {
        version: '1.5.2',
        date: '2026-08-23',
        changes: [
            'Разделы сборника проверяются при сборке: песня не может потеряться или попасть в два раздела'
        ]
    },
    {
        version: '1.5.1',
        date: '2026-08-23',
        changes: [
            'Выбранный способ группировки на экране «Все песни» запоминается'
        ]
    },
    {
        version: '1.5.0',
        date: '2026-08-23',
        changes: [
            'Экран «Все песни»: группировка по номеру, по алфавиту и по разделам сборника',
            'Поиск в верхней панели на этом экране'
        ]
    },
    {
        version: '1.4.0',
        date: '2026-08-12',
        changes: [
            'Блок «Состояние хранилища» на этой странице: сколько песен и подборок в базе и цела ли резервная копия'
        ]
    },
    {
        version: '1.3.0',
        date: '2026-08-11',
        changes: [
            'Экспорт подборок в файл и импорт обратно в настройках'
        ]
    },
    {
        version: '1.2.0',
        date: '2026-08-11',
        changes: [
            'Резервная копия подборок снимается сама; после потери данных приложение предлагает восстановить'
        ]
    },
    {
        version: '1.1.4',
        date: '2026-08-11',
        changes: [
            'Приложение просит у браузера постоянное хранилище — данные реже удаляются при нехватке места'
        ]
    },
    {
        version: '1.1.3',
        date: '2026-08-11',
        changes: [
            'Сбой обновления базы больше не оставляет приложение с пустым экраном'
        ]
    },
    {
        version: '1.1.2',
        date: '2026-08-10',
        changes: [
            'Убран лишний скролл страницы при скрытой системной навигации'
        ]
    },
    {
        version: '1.1.1',
        date: '2026-08-07',
        changes: [
            'Исправлен скачок страницы при открытии экранной клавиатуры на Android'
        ]
    },
    {
        version: '1.1.0',
        date: '2026-08-07',
        changes: [
            'Версия и дата сборки на странице «О приложении»'
        ]
    }
]

/**
 * Сколько версий показывать до нажатия «Показать все».
 *
 * Список длинный и растёт, а интересны обычно последние: разворот прячет
 * историю, но не выкидывает её.
 */
export const CHANGELOG_PREVIEW = 3

/**
 * Дата версии в виде `ДД.ММ.ГГГГ`.
 *
 * Форматируем вручную, а не через `toLocaleDateString`: строка одинаково
 * выглядит на любом устройстве — её пересылают скриншотом вместе с версией.
 *
 * @param {string} iso — дата в формате `YYYY-MM-DD`
 * @returns {string} пустая строка, если формат не тот
 */
export const formatChangelogDate = (iso) => {
    const match = /^(\d{4})-(\d{2})-(\d{2})$/.exec(String(iso || ''))

    if (!match) return ''

    const [, year, month, day] = match

    return `${day}.${month}.${year}`
}

/**
 * Записи для показа: либо все, либо первые `limit`.
 *
 * @param {ChangelogEntry[]} entries
 * @param {boolean} expanded
 * @param {number} limit
 * @returns {ChangelogEntry[]}
 */
export const visibleChangelog = (entries, expanded, limit = CHANGELOG_PREVIEW) => {
    const list = Array.isArray(entries) ? entries : []

    return expanded ? list : list.slice(0, Math.max(0, limit))
}

/**
 * Есть ли что разворачивать — кнопка «Показать все» без этого бессмысленна.
 *
 * @param {ChangelogEntry[]} entries
 * @param {number} limit
 * @returns {boolean}
 */
export const hasMoreChangelog = (entries, limit = CHANGELOG_PREVIEW) => (
    (Array.isArray(entries) ? entries.length : 0) > Math.max(0, limit)
)
