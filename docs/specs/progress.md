# План доработок по спецификации v1

Последнее обновление: 2026-05-22

## Статус задач

### Приоритет 1 — Критичный UI + Функциональность

| # | Задача | Статус | Примечания |
|---|--------|--------|------------|
| 1 | Редизайн SongDisplay: «Лист песни» с маркерами куплетов/припевов | ЧАСТИЧНО | Куплеты/припевы сделаны, нет подсветки повторов и max-width для мелкого шрифта |
| 2 | Боковое меню: подборки в меню, настройки внизу | ГОТОВО | Подборки в сайдбаре, Избранное со звёздочкой, настройки внизу, скролл, кнопка закрытия на месте гамбургера, Teleport `#navbar-left` для гамбургера/назад |
| 3 | Страница подборки: стиль поиска + режим редактирования | ГОТОВО | Режим редактирования, стиль как в поиске, удаление подборки (кроме «Избранного») |
| 4 | Главная: инструкция под поиском | ГОТОВО | 3 пункта при пустом избранном, краткая подсказка иначе, иконка mingcute:star-line, шрифт 0.95rem |
| 5 | Парсер повторов `/.../Nр.` с вложенностью (стек) | НЕ НАЧАТО | |
| 6 | Подсветка повторов в UI | НЕ НАЧАТО | Зависит от #5 |
| 7 | Опция «Не гасить экран» | НЕ НАЧАТО | |
| 8 | Автообновление: проверка хеша, toast + бейдж | НЕ НАЧАТО | Есть только автозагрузка при пустой БД |

### Приоритет 2 — Хранение данных

| # | Задача | Статус | Примечания |
|---|--------|--------|------------|
| 9 | Создать репозиторий `songs-data` с YAML-файлами | НЕ НАЧАТО | |
| 10 | Написать парсер YAML → JSON | НЕ НАЧАТО | |
| 11 | Настроить CI/CD: склейка + fetch + конвертация | НЕ НАЧАТО | |
| 12 | Расставить переносы строк в YAML-файлах | НЕ НАЧАТО | Зависит от #9 |
| 13 | Таблица `songSettings` + транспонирование аккордов | НЕ НАЧАТО | DB version 6, но хранилище songSettings не создано |

### Приоритет 3 — Аккорды

| # | Задача | Статус | Примечания |
|---|--------|--------|------------|
| 14 | Расстановка аккордов в YAML-файлах | НЕ НАЧАТО | Зависит от #9 |
| 15 | UI транспонирования на странице песни | НЕ НАЧАТО | Зависит от #13 |

### Приоритет 4 — Полировка

| # | Задача | Статус | Примечания |
|---|--------|--------|------------|
| 16 | Дедупликация toggle-switch CSS | ГОТОВО | Коммит 8371b0d |
| 17 | Удаление мёртвого CSS (.nav-spacer) | ГОТОВО | Коммит 8371b0d |
| 18 | Горизонтальный режим: max-width контента | ЧАСТИЧНО | Только medium/large шрифты, нет для small |
| 19 | Хардкод цветов → CSS-переменные | ЧАСТИЧНО | Остались: `#f59e0b` (звезда), `#ccc` (slider off), `#fff` (кнопка установки) |

---

## Детали по задачам

### #1 SongDisplay — что осталось
- Подсветка повторов (зависит от #5 и #6)
- max-width для мелкого шрифта при широких экранах

### #2 Боковое меню — ГОТОВО
- ✅ Вывести список подборок в сайдбаре (с количеством песен)
- ✅ «Избранное» — первое со звёздочкой, системная подборка
- ~~Кнопка `[+]` для создания подборки~~ — пока не делаем
- ✅ Настройки прижать к низу сайдбара
- ✅ Скролл секции подборок при большом количестве
- ✅ Кнопка закрытия (X) в хедере сайдбара, на месте гамбургера
- ✅ Teleport `#navbar-left` — страницы сами решают, что слева: гамбургер (главная, песня, подборки) или стрелка назад (настройки, конкретная подборка)
- ✅ `provide('toggleSidebar')` / `inject('toggleSidebar')` — страницы вызывают открытие сайдбара
- Бейдж на иконке настроек при наличии обновления (связано с #8) — пока не сделано

### #4 Главная — ГОТОВО
- ✅ Расширенная инструкция (3 пункта с буллетами) при пустом избранном
- ✅ Краткая подсказка, если избранное уже есть
- ✅ Иконка mingcute:star-line вместо текстового ★
- ✅ Крупный шрифт (0.95rem)

### #3 Страница подборки — ГОТОВО
- ✅ Режим редактирования: кнопка «Редактировать» → «Готово»
- ✅ В режиме редактирования: красные кнопки удаления у каждой песни, клик по песне отключён
- ✅ Стилизация списка песен как в результатах поиска (номер + название + badge)
- ✅ Кнопка удаления подборки (скрыта для «Избранного»)

### #4 Главная — ГОТОВО
- Текст-инструкция под полем поиска
- Если избранное пустое — более развёрнутая инструкция

### #5 Парсер повторов
- Синтаксис: `/` — начало повтора, `/Nр.` — конец (N — количество раз)
- Вложенность через стек
- Интеграция в `processContent()` SongDisplay или в отдельную функцию
- `cleanText()` в `lib/search.js` уже удаляет `/` — при поиске повторы не учитываются

### #6 Подсветка повторов в UI
- Курсив + цвет `--text-secondary`
- Не разворачивать повторы

### #7 Не гасить экран
- Toggle в настройках (по умолчанию: вкл)
- `navigator.wakeLock.request('screen')`
- Добавить `keepScreenOn` в Pinia store (`stores/settings.js`)
- Приоритет: Android (Chrome/Edge), iOS — по возможности

### #8 Автообновление
- При запуске: проверка хеша/etag `songs.json`
- Если отличается: toast + бейдж на иконке настроек
- Пользователь сам запускает обновление
- Если обновление <10 сек — можно обновлять сразу при подтверждении

### #13 songSettings + транспонирование
- Новое хранилище в IndexedDB: `songSettings` (keyPath: `songNumber`, поля: `transpose`)
- Диапазон транспонирования: -6..+5 полутонов
- Функции в `useIndexDB`: `getSongSettings(number)`, `setTranspose(number, value)`
- Обновить dbVersion (сейчас 6, но songSettings нет)

### #18 Горизонтальный режим — что осталось
- Добавить max-width для `.font-size-small` при `min-width: 1024px`
- Аналогично для `.song-title-row` в `song/[number].vue`

### #19 Хардкод цветов — что осталось
- `#f59e0b` → CSS-переменная (звезда избранного, `song/[number].vue`)
- `#ccc` → CSS-переменная (slider off, `settings.vue`)
- `#fff` → CSS-переменная (кнопка установки, `index.vue`)

---

## Рекомендуемый порядок реализации

1. ~~**#2 Боковое меню**~~ — ГОТОВО
2. ~~**#4 Главная: инструкция**~~ — ГОТОВО
3. ~~**#3 Страница подборки: режим редактирования**~~ — ГОТОВО
4. **#7 Не гасить экран** — простая фича, важная для пения
5. **#5 + #6 Повторы** — парсер + подсветка вместе
6. **#8 Автообновление** — сложнее, но важно для актуальности данных
7. **#13 + #15 Транспонирование** — songSettings + UI
8. **#18 + #19 Полировка** — остатки P4
